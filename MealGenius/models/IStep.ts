export interface Step {
    /** The id of the step. */
    id: string;
    /** The description of the step. */
    description: string;
    /** The number of the step. */
    number: number;
    /** The duration of the step. */
    duration: number;
}